services:
  - type: web
    name: zovo-backend
    env: node
    plan: free
    buildCommand: cd zovo-backend && npm install
    startCommand: cd zovo-backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
